# CNC CAM 仿真软件优化方案

## 一、当前状态

本软件已实现 CNC CAM 与 G代码仿真的核心闭环：

- **DXF 读取**：LINE / CIRCLE / ARC / LWPOLYLINE / POLYLINE / SPLINE，支持图层过滤、INSERT 块展开
- **G代码解析**：G0/G1/G2/G3、G17/G18/G21/G90/G91/G94、I/J/K/R 圆弧、增量坐标、模态继承
- **CAM 生成**：车削/铣削模式、坐标归零、轮廓方向、圆弧输出策略、刀具半径补偿（左/右）
- **仿真**：2D 刀路、3D 车削/铣削可视化、动画播放、时间估算、速度调节
- **UI**：工业风格面板、G代码编辑器、参数面板、时间线、登录验证、项目导航
- **工程化**：模块化结构、36 个核心测试用例、`README.md`、PyInstaller 打包

当前代码总量约 **5700 行**，36 个测试全部通过。项目已从“可运行演示”进入“工程化打磨”阶段，以下按优先级列出需要修复的问题与优化方向。

---

## 二、P0 级问题（必须修复）

### 2.1 DXF 读取模块中的中文乱码

**问题**：`core/dxf_reader.py` 中多处将中文标点写成了字面量 `?`，导致状态栏和错误提示出现乱码。例如：

```python
f"LINE {len(self.lines)} \u4e2a?"     # 实际输出：LINE 0 个?
raise ValueError(f"\u65e0\u6cd5\u8bfb\u53d6DXF\u6587\u4ef6?{exc}")  # 实际输出：无法读取DXF文件?xxx
```

实际运行 `DxfReadResult().summary()` 会返回：

```text
'LINE 0 个?CIRCLE 0 个?ARC 0 个?POLYLINE 0 个'
```

**修复方案**：
- 统一使用中文标点或英文标点，删除字面量 `?`：
  - `"LINE {n} 个，CIRCLE {n} 个，ARC {n} 个，POLYLINE {n} 个"`
  - `"无法读取 DXF 文件：{exc}"`
  - `"{dxftype} 图元读取失败：{exc}"`
- 全项目搜索 `\uXXXX?` 或 `?{` 模式，统一修复。

### 2.2 `ControlPanel.set_available_layers` 重复创建控件

**问题**：`ui/control_panel.py:61-65` 在刷新图层列表时，又新建了一个 `cutter_compensation_input`：

```python
def set_available_layers(self, layers: list[str]) -> None:
    ...
    self.cutter_compensation_input = QComboBox(self)   # 错误：重复创建，未加入布局
    self.cutter_compensation_input.addItem("无补偿", "none")
    ...
```

这会导致：
- 布局中原有的刀具补偿下拉框与代码逻辑断开；
- 新控件游离在界面之外，用户看不到但 `parameters()` 可能读到它的值；
- 多次导入 DXF 会产生多个游离控件。

**修复方案**：
- 删除 `set_available_layers` 中创建 `cutter_compensation_input` 的代码；
- 刀具补偿控件在 `__init__` 中创建一次即可，后续不再重建；
- 为所有 `QComboBox` 等输入控件添加自动化测试，防止类似回归。

### 2.3 3D 画布类名与实际实现不符

**问题**：`ui/gl_canvas_widget.py` 中的 `GLCanvasWidget` 实际使用 matplotlib 3D 投影，并未使用 OpenGL。类名和文件名会误导后续维护者。

**修复方案**：
- 将类重命名为 `Simulation3DCanvasWidget` 或 `Mpl3DCanvasWidget`；
- 同步修改 `ui/main_window.py` 中的引用；
- 若未来需要真正的 OpenGL 渲染，可新建 `GLCanvasWidget` 实现，保持接口兼容。

---

## 三、P1 级问题（建议修复）

### 3.1 坐标/插值逻辑重复

**问题**：`core/simulator.py` 已经实现了 `_next_position` 和坐标变换，`ui/simulation_runner.py` 又内部实现了 `_MachinePos.advance()`，两者逻辑几乎相同。如果后续支持 G92、G52、G54-G59 等坐标系偏移，需要改两处。

**修复方案**：
- 将“机器坐标状态机”抽到 `core/machine_state.py` 或复用 `simulator` 中的位置推进逻辑；
- `SimulationRunner` 只负责插值和动画调度，不负责重新解析 G代码语义。

### 3.2 `geometry_utils.py` 利用率低

**问题**：`core/geometry_utils.py` 目前只定义了一个 `Point2D`，而 `DxfLine`、`ToolpathPoint`、canvas 绘制中都在各自重复实现点/向量结构。

**修复方案**：
- 扩展 `geometry_utils.py` 为通用几何工具包：
  - `Point2D`、`Point3D`、`Vector2D`
  - `distance`、`lerp`、`signed_area`、`is_close`、`line_intersection` 等
- 让 `DxfLine`、`ToolpathPoint`、CAM 偏置、canvas 绘制统一复用或基于同一模型。

### 3.3 魔法常量分散

**问题**：采样数、容差、默认参数、颜色值等硬编码分散在各文件中，例如：

```python
CIRCLE_SEGMENT_COUNT = 72
ARC_DEGREE_STEP = 5.0
LINE_JOIN_TOLERANCE = 1e-3
DEFAULT_RAPID_SPEED = 50000.0
```

**修复方案**：
- 新建 `core/constants.py`（工程常量）和 `ui/theme.py`（颜色/样式常量）；
- 将分散的常量集中管理，便于后续调参和主题切换。

### 3.4 登录框硬编码密码

**问题**：`ui/login_dialog.py` 中 `DEMO_PASSWORD_SHA256` 是写死的 SHA256 摘要，仅适合课堂演示，不能作为正式访问控制。

**修复方案**：
- 短期：将密码校验改为可选配置，默认不启用登录框；
- 长期：如需权限管理，使用 QSettings 保存用户配置，或接入真实认证接口。

### 3.5 测试框架非标准化

**问题**：`tests/test_core_workflow.py` 使用手写 `main()` 循环，没有使用 `pytest` 或 `unittest`。缺少参数化、fixture、失败时的详细 traceback。

**修复方案**：
- 引入 `pytest` 和 `pytest-qt`；
- 将现有测试函数改为 `pytest` 风格，使用 fixture 管理 `QApplication` 和 `MainWindow` 生命周期；
- 添加 `pytest.ini`，配置 `QT_QPA_PLATFORM=offscreen` 和 `MPLCONFIGDIR`。

### 3.6 异常处理与日志不一致

**问题**：部分关键路径缺少日志，某些异常被静默吞掉；`ui/main_window.py` 和 `ui/gl_canvas_widget.py` 中仍有裸 `type: ignore[no-untyped-def]`。

**修复方案**：
- 统一引入 `logging` 模块，在 DXF 读取、G代码解析、CAM 生成等关键路径记录 `warning`/`error`；
- 对 matplotlib 事件处理函数补全类型签名，减少 `type: ignore`；
- 避免裸 `except Exception`，捕获具体异常类型。

---

## 四、P2 级问题（功能增强）

### 4.1 CAM 算法深化

**问题**：当前 CAM 仅支持轮廓加工，缺少型腔（pocket）、钻孔循环、螺旋下刀等常见策略。

**修复方案**：
- 在 `cam_generator.py` 中抽象 `Operation` 基类；
- 逐步添加：
  - 外轮廓/内轮廓精加工
  - 型腔粗加工（Zig-Zag / 轮廓偏置）
  - 钻孔（G81/G83 循环）
- 每种策略生成独立的工序节点，接入左侧工序表。

### 4.2 刀具半径补偿健壮性

**问题**：当前 `_offset_closed_polyline` 对简单凸轮廓有效，但对内凹角、自相交轮廓处理有限。

**修复方案**：
- 使用成熟几何库（如 `shapely`）或自研 offset 算法处理复杂轮廓；
- 对自相交、偏置后轮廓退化等情况给出明确警告；
- 在 2D 画布中预览补偿后的刀路，方便用户核对。

### 4.3 2D/3D 画布性能优化

**问题**：
- 2D 画布中每段刀路独立绘制，大规模程序较慢；
- 3D 画布每帧 `self._axis.clear()` 全量重绘。

**修复方案**：
- 2D：同类型线段合并为 `LineCollection`，圆弧采样点批量绘制；
- 3D：
  - 静态几何（毛坯、完整刀路）缓存；
  - 仅更新刀具位置和已切削区域；
  - 路径点超过阈值时自动降采样。

### 4.4 G代码解析器扩展

**问题**：解析器支持的指令集仍偏基础，缺少常见 CNC 功能。

**修复方案**：
- 支持 `G43 Hxx` 刀具长度补偿；
- 支持 `G54-G59` 工件坐标系；
- 支持 `M08/M09` 冷却液、`M06` 换刀；
- 行号 `Nxxxx` 与编辑器高亮对齐。

### 4.5 工程文件保存/打开

**问题**：软件无法保存项目状态，每次启动需重新导入 DXF 和配置参数。

**修复方案**：
- 设计 `.cncproj` 项目文件格式（JSON）；
- 保存：导入的 DXF 路径、CAM 参数、生成的 G代码、最近仿真结果；
- 打开：恢复参数面板和编辑器内容。

---

## 五、P3 级问题（长期规划）

### 5.1 真实加工时间估算

- 引入加速/减速（S-curve）模型；
- 区分快速移动与实际切削的耗时；
- 按刀具直径、切宽、切深估算材料去除率。

### 5.2 G代码后处理配置

- 支持不同数控系统（Fanuc、Siemens、华中数控、广州数控）的程序头尾差异；
- 换刀指令、冷却液、坐标系指令可配置。

### 5.3 仿真碰撞检测

- 检测刀具与夹具的碰撞；
- 检测过切与残留；
- 对检测到的问题高亮显示并记录日志。

### 5.4 真正的 OpenGL 3D 渲染

- 当路径规模较大时，matplotlib 3D 会卡顿；
- 引入 PyOpenGL / VisPy / PyQtGraph 实现高性能 3D 仿真。

---

## 六、验收标准

### 完成 P0 后应满足

1. 所有测试继续通过（当前 36 个）。
2. DXF 导入后的状态栏中文显示正常，无乱码问号。
3. 多次导入 DXF 后，刀具补偿下拉框仍正常工作。
4. `GLCanvasWidget` 类名与实现一致，或已明确重命名。

### 完成 P1 后应满足

1. `SimulationRunner` 不再重复实现坐标状态机。
2. `geometry_utils.py` 成为公共几何工具入口。
3. 分散的魔法常量已集中到 `core/constants.py` 和 `ui/theme.py`。
4. 测试已迁移到 `pytest` 框架。
5. 关键路径有日志输出，无裸 `except Exception`。

### 完成 P2 后应满足

1. 简单轮廓可生成带刀补的刀路并预览。
2. 2D/3D 画布在大规模程序下仍保持流畅。
3. G代码解析器支持 `G43`/`G54-G59`/`M08/M09` 等常用指令。
4. 支持保存/打开工程文件。

---

## 七、实施顺序建议

| 阶段 | 内容 | 预计耗时 |
|---|---|---|
| 第 1 阶段 | 修 P0：中文乱码、控件重复创建、3D 画布命名 | 0.5-1 天 |
| 第 2 阶段 | P1 工程化：抽几何工具、统一常量、迁 pytest、加日志 | 1.5-2.5 天 |
| 第 3 阶段 | P2 增强：CAM 策略、刀补健壮性、画布性能、G代码扩展 | 3-5 天 |
| 第 4 阶段 | P3 规划：后处理、碰撞检测、OpenGL 选型 | 视项目周期而定 |

---

## 八、风险提醒

- **刀补偏置**属于几何算法，实现时需注意自相交、内凹角和轮廓方向，建议先做简单凸轮廓，再逐步扩展。
- **3D 画布性能优化**需保留当前交互体验，避免影响 Matplotlib 3D 导航。
- **类名重命名**属于破坏性变更，需同步更新 `ui/main_window.py` 和所有相关测试。
- 任何改动后必须运行测试套件，确保 36 个测试全部通过。
