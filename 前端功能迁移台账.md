# CNC CAM 新前端功能迁移台账

本文件用于记录“把高保真 CAM 工作站设计迁移回原 PySide6 主程序”的长期工作。每完成一个阶段后先回看本文件，再继续下一项。

## 当前目标

把项目中已经存在的真实能力保留在 `py main.py` 打开的桌面软件里，同时把界面升级到高保真 CAM 工作站标准：

- DXF 读取与图元预览
- DXF 到 CAM G代码生成
- G代码解析
- G0/G1/G2/G3 二维仿真
- 解析/仿真统计与警告显示
- 参数面板联动

之后再逐步补全概念图里新增但旧项目尚未实现的功能。

## 已完成

- [x] 建立高保真浏览器前端原型：`frontend_sandbox/index.html`
- [x] 建立工业 CAM 风格视觉系统：`frontend_sandbox/styles.css`
- [x] 建立 canvas 刀路绘制与基础交互：`frontend_sandbox/app.js`
- [x] 创建长期迁移台账：`前端功能迁移台账.md`
- [x] 梳理现有核心模块能力：
  - `core/dxf_reader.py`
  - `core/gcode_parser.py`
  - `core/simulator.py`
  - `core/cam_generator.py`
  - `core/toolpath.py`
- [x] 创建轻量 Python HTTP 服务：`frontend_sandbox/server.py`
- [x] 增加示例文件 API：`GET /api/examples`
- [x] 增加示例 G代码读取 + 解析 + 仿真 API：`GET /api/gcode/example`
- [x] 增加示例 DXF 读取 API：`GET /api/dxf/example`
- [x] 增加手动 G代码仿真 API：`POST /api/simulate`
- [x] 增加 DXF 到 CAM G代码生成 API：`POST /api/cam/generate`
- [x] 前端启动后自动加载 `examples/arc_test.nc` 并绘制真实 G2/G3 仿真结果
- [x] 前端“导入DXF”按钮接入 `examples/rectangle.dxf`
- [x] 前端“生成G代码”按钮接入真实 `CamGenerator`
- [x] 前端“开始仿真”按钮接入真实 `GCodeParser + GCodeSimulator`
- [x] 用户确认：后续不再以浏览器沙盒为目标，改为升级 `py main.py` 打开的 PySide6 主程序
- [x] 新增 PySide6 工作站面板模块：`ui/workstation_panels.py`
- [x] `ui/main_window.py` 改造成左侧工程导航 + 中央画布/控制台 + 右侧 CAM 检查器布局
- [x] `ui/control_panel.py` 改造成右侧纵向 CAM 参数检查器，并显示原有状态指标
- [x] `ui/style.py` 增加工业 CAM 工作站风格样式
- [x] 左侧面板已能显示项目树、DXF 图元摘要表、工序表、状态概览
- [x] 底部时间线面板已能显示播放控制、进度、统计指标、警告日志
- [x] PySide 主窗口编译检查通过：`py -m compileall main.py ui core`
- [x] PySide 主窗口 offscreen 实例化冒烟测试通过
- [x] 用户确认：浏览器沙盒不再需要，已删除 `frontend_sandbox/`
- [x] 清理旧打包输出与缓存：`build/`、`dist/`、`__pycache__/`
- [x] 新增测试 DXF：`examples/slot_plate_test.dxf`
- [x] 新增测试 DXF：`examples/turning_profile_test.dxf`
- [x] 两个新增 DXF 已通过 `DxfReader` 读取验证，警告 0 条
- [x] 重新打包 EXE 完成：`dist/CNC_CAM_Simulator/CNC_CAM_Simulator.exe`
- [x] 确认新 DXF 测试文件已进入 EXE 资源目录：`dist/CNC_CAM_Simulator/_internal/examples/`
- [x] 打包后清理 PyInstaller 中间目录：`build/`
- [x] 修复车削/铣削坐标轴映射分离：车削使用 `G18 X/Z I/K`，铣削使用 `G17 X/Y I/J`
- [x] 修复对称车削轮廓公式：DXF 横向 X -> 车床 Z，DXF 纵向 Y 半径 -> 车床 X 直径
- [x] 修复闭合对称车削轮廓的上半轮廓提取顺序，避免刀路在 Z 方向来回跳跃
- [x] 生成 G代码时输出标准程序号、模态头、N 行号和结束段
- [x] 重新打包包含车削修复的 EXE：`dist/CNC_CAM_Simulator/CNC_CAM_Simulator.exe`
- [x] 打包后再次清理 PyInstaller 中间目录和 Python 缓存
- [x] 修复车削仿真显示投影：XZ 平面按机床习惯显示为横轴 Z、纵轴 R
- [x] 修复工业常用主轴示例 G代码的肩部圆角方向，半径一致性校验通过
- [x] 车削画布升级为回转体截面视图：G代码 X 直径显示时换算为 R 半径，并镜像中心线下半轮廓
- [x] 重新打包包含回转体截面显示的 EXE：`dist/CNC_CAM_Simulator/CNC_CAM_Simulator.exe`

## 进行中

- [ ] 将左侧 DXF 图元摘要表进一步升级为真实 DXF 图层表
- [ ] 将工序列表从静态摘要升级为可编辑真实工序数据
- [ ] 将底部仿真时间线从结果摘要升级为逐行播放
- [ ] 优化 PySide 画布细节，使其更接近概念图里的专业 CAM 画布

## 待迁移的旧功能

- [x] 导入示例 G代码文本并解析
- [x] 导入示例 DXF 文件并读取图元
- [x] 显示 DXF 图纸范围、图元数量、读取警告
- [x] 根据 CAM 参数生成 G代码
- [x] 支持铣削模式：X/Y 平面，程序头使用 `G17`
- [x] 支持圆弧输出：`G2/G3 圆弧`
- [x] 支持 G2/G3 仿真：I/J 或 I/K 圆心偏移
- [x] 支持坐标归零
- [x] 支持 LINE 轮廓合并
- [ ] 支持真实文件上传导入 G代码
- [ ] 支持真实文件上传导入 DXF
- [x] 支持车削模式：DXF X/Y 映射为 G代码 X/Z，程序头使用 `G18`
- [ ] 支持圆弧输出：折线近似
- [ ] 支持画布缩放、平移、适配视图
- [ ] 支持仿真信息弹窗或右侧信息检查器
- [ ] 支持保存生成后的 G代码

## 新前端中需要逐步补齐的新功能

- [ ] 项目树真实数据
- [ ] DXF 图层表真实图层、颜色、可见性
- [ ] 工序列表增删改、排序
- [ ] 刀具库与刀具选择
- [ ] 刀路质量检查：未闭合轮廓、切深风险、圆弧参数异常
- [ ] 仿真时间线逐行播放
- [ ] 当前命令、模态信息、当前坐标实时联动
- [ ] 报警/错误日志
- [ ] 视图控制：网格、坐标轴、刀路、快移、边界框开关
- [ ] 后处理输出配置
- [ ] 工程文件保存/恢复

## 当前限制

- 浏览器沙盒仅作为设计参考，不再作为当前阶段交付目标。
- 暂不做系统级文件管理和危险写入操作。
- `frontend_sandbox/` 的 API 服务优先使用项目内 `examples/` 文件验证真实功能。
- Browser / Chrome 插件本轮不可用，验证使用 API 请求和 Edge 无头截图完成。

## 下一步

1. 改造 `ui/main_window.py` 主体布局。
2. 将 `ui/canvas_widget.py` 的画布视觉升级为概念图标准：尺寸标注、边界框、视图控制、图例。
3. 将时间线面板接入逐行/逐段播放状态。
4. 将 DXF Reader 扩展出图层/颜色信息，并填充左侧真实图层表。
